backend:
  name: git-gateway
  branch: master
publish_mode: editorial_workflow
media_folder: content/projects/images
public_folder: /images

collections:
  - name: 'projects'
    label: 'Project'
    folder: 'content/projects'
    create: true
    slug: '{{slug}}'
    editor:
      preview: true
    fields:
      - { label: 'Project url', name: 'slug', widget: 'string' }
      - { label: 'Project title', name: 'title', widget: 'string' }
      - { label: 'Project category', name: 'category', widget: 'string' }
      - { label: 'Project description', name: 'description', widget: 'string' }
      - { label: 'Article body', name: 'body', widget: 'markdown' }
